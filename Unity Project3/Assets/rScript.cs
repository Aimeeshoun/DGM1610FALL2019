using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rScript : MonoBehaviour
{
    public int firstInt = 1;

    public string firstString = "Hello World!";

    public char firstChar = 'a';

    public bool firstBool = false;
    public double firstDouble = 2.39475;

    public float firstFloat = 2f;
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
